I'm adding a new feature to my existing FastAPI + LangChain multi-agent system project. 

CONTEXT: This project has a Planner Agent that decomposes free-text tasks into 
structured JSON execution plans, and a Worker Agent that executes each step with 
retry logic, streaming results back via Server-Sent Events (SSE). Currently 
everything runs synchronously in-memory.

FEATURE TO ADD: A queuing layer for heavy inputs.
- Add a SQLAlchemy model called `Job` with fields: id (UUID or int), input_text, 
  status (queued/processing/done/failed), result (nullable text), created_at, 
  updated_at.
- In the existing task-submission endpoint, check input size (e.g. word count > 
  500, or make this a configurable threshold). If it exceeds the threshold:
  - Insert a new Job row with status "queued"
  - Return the job ID immediately to the client (don't block)
  - If under the threshold, keep the existing synchronous SSE streaming behavior 
    unchanged.
- Add a background worker (use FastAPI BackgroundTasks for simplicity, or a 
  simple polling loop) that:
  - Picks up jobs with status "queued"
  - Updates status to "processing"
  - Runs the existing Planner Agent -> Worker Agent pipeline on the input
  - Saves the final result and sets status to "done" (or "failed" with an error 
    message if it errors)
- Add a GET /jobs/{job_id} endpoint that returns the job's current status and 
  result if done.
- Use PostgreSQL as the database (I'll provide the connection string — use 
  SQLAlchemy with psycopg2, read DATABASE_URL from an environment variable, 
  don't hardcode it).
- Write 2-3 PyTest tests: one for job creation, one for the status-check 
  endpoint, one for the size-threshold routing logic (heavy input creates a Job 
  row, light input doesn't).
- Don't break or modify the existing SSE streaming endpoint — this should be an 
  additive parallel path, not a replacement.

Walk me through the SQLAlchemy model first, then the routing logic, then the 
background worker, then the tests — one piece at a time so I understand and can 
verify each part before moving to the next.